/**
 * 陣
 */
const Fields = (() => {
  let first = 'first';
  let second = 'second';
  let center = 'center';

  return {
    FIRST : first
    , SECOND : second
    , CENTER : center
  }
})();

export default Fields;
